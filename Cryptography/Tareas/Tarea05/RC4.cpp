#include <bits/stdc++.h>

#define SIZE 32

using namespace std;

void f(){
    vector<short> S(255, 0);
    vector<short> T(255, 0);
    vector<short> K(255, 0);
    for(int i = 0; i < 255; i++){
        S[i] = i;
        T[i] = K[i % k]
    }
}

int main(){

    return 0;
}