package Automaton;

public class StatesProtocol {
    
    public int q0(char character, int actualState){
        
        //Here is possible know if the protocol is available or not
        if(Math.rint(Math.random()) == 1){
            
        } else
            System.out.println("Protocol isn't available");
        
        return actualState;
    }
}
